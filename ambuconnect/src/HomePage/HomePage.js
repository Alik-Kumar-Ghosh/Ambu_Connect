import React, { memo } from "react";
import Landing from "../HomePage/Landing";

const HomePage = () => {
  return (
    <div>
      <Landing />
    </div>
  );
};

export default memo(HomePage);
